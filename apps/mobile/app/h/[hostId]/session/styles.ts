import { mobileSessionCommandInputStyles } from './command-input-styles'
import { mobileSessionFrameStyles } from './frame-styles'
import { mobileSessionReaderStyles } from './reader-styles'
import { mobileSessionReviewCommentStyles } from './review-comment-styles'

export const styles = {
  ...mobileSessionFrameStyles,
  ...mobileSessionReaderStyles,
  ...mobileSessionReviewCommentStyles,
  ...mobileSessionCommandInputStyles
}
