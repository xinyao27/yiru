// Why: the workspace column leaves its sidebar edge open so the first tab
// meets that boundary directly; split panes still paint their own separators.
export const WORKSPACE_COLUMN_FRAME_CLASS_NAME =
  'flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden'

export const WORKSPACE_COLUMN_BODY_CLASS_NAME = 'flex min-h-0 min-w-0 flex-1 overflow-hidden'
